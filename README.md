# fabric-socialpulse-lakehouse-platform
End-to-end data engineering pipeline built on Microsoft Fabric implementing Medallion Architecture (Bronze–Silver–Gold) to ingest API data, perform data quality validation, transform data using PySpark notebooks, and generate analytics-ready datasets for reporting.
